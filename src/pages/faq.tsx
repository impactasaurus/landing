import * as React from "react";
import {withLayout} from "../components/Layout";

const FAQPage = () => {
  return (
    <div>
      TODO - FAQ
    </div>
  );
};

export default withLayout(FAQPage);
